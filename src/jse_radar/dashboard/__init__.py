"""
Plotly Dash dashboard for jse-radar.

Entry point:
    from jse_radar.dashboard.app import create_app
    app = create_app()
    app.run(debug=True)

Or run directly:
    python -m jse_radar.dashboard.app
"""